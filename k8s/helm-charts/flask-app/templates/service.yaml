apiVersion: v1
kind: Service
metadata:
  name: {{ include "quake_watch_app-chart.fullname" . }}
  labels:
    {{- include "quake_watch_app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "quake_watch_app-chart.selectorLabels" . | nindent 4 }}
