server:
  persistentVolume:
    enabled: true
    size: 10Gi
  resources:
    requests:
      cpu: "200m"
      memory: "512Mi"
    limits:
      cpu: "1"
      memory: "2Gi"
  service:
    type: ClusterIP
alertmanager:
  enabled: false
  service:
    type: ClusterIP